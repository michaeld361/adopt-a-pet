# Server port
PORT=3000

# Comma-separated list of allowed origins for CORS, or * for all
CORS_ORIGINS=*

# Path to dog images directory (defaults to ./dog-images)
# DOG_IMAGES_DIR=/path/to/dog-images

# Path to uploads directory (defaults to ./uploads)
# UPLOADS_DIR=/path/to/uploads
